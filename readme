1) The Python code write data into Partitions of BMI Category to facilitate the query on Obese Category (Partition Pruning)
2) Partitioned the raw data into 200 partitions to facilitate parallel computations on each partition
3) Used python unittest module to read test data and assert the expected output
4) Parameterized the input and output path
5) AUTOMATED the trigger of git checkout, Unit Test execution, Build and Execute by using Jenkins locally. Refer the Jenkinsfile for script.